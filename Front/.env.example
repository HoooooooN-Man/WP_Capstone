# Front/.env.example
# 이 파일을 .env 또는 .env.local 로 복사해서 사용.

# ML 분석 서버 (FastAPI :8001) — 종목 추천, 차트, 재무, 스크리너, 게시판, 마켓 레이더
VITE_API_BASE_ML=http://localhost:8001

# 인증·뉴스·커뮤니티 서버 (FastAPI :8000) — auth, news, board(asyncpg), users
VITE_API_BASE_AUTH=http://localhost:8000
